import {
  createdBanner,
  delBanner,
  updateBanner,
  getBanner,
} from '@/api/banner';

const banner = {
  state: {},

  mutations: {},

  actions: {
    createdBanner,
    delBanner,
    updateBanner,
    getBanner,
  },
};

export default banner;
